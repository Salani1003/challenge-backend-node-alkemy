/* const {Router} = require("express");
const {
  postGenero,
  getGenero,
  putGenero,
  deleteGenero,
} = require("../controllers/genero");

const router = Router();

router.get("/", getGenero);
router.post("/", postGenero);
router.put("/", putGenero);
router.delete("/", deleteGenero);

module.exports = router;
 */
